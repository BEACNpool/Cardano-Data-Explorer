# Scripts

Put operational scripts here (export, transfer, import, validate, ETL orchestration).

Safety baseline:
- read-only checks can run unattended
- write/destructive actions should require explicit approval

Suggested future scripts:
- `export_snapshot.sh`
- `transfer_snapshot.sh`
- `import_snapshot.sh`
- `run_pipeline.sh`
- `validate_snapshot.sh`
